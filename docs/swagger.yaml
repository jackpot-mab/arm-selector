info:
  contact: {}
paths:
  /select:
    get:
      consumes:
      - application/json
      description: select arm
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: select arm
      tags:
      - arm-selector
swagger: "2.0"
